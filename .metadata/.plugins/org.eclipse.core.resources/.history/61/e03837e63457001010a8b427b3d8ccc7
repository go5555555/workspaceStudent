<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>科目フォーム</title>
</head>
<body>
	<h2 th:if="${subjectForm.isNewSub}">科目新規登録</h2>
	<h2 th:unless="${subjectForm.isNewSub}">科目名編集</h2>
	
	<form th:action="${subjectForm.isNewsub}? @{/subjects/save}:@{/subjects/update}"
		th:object="${subjectForm}" method="post">
		<input type="hidden" th:field="*{id}">
		
		<table border="1" th:unless="${subjectForm.isNew}">
			<tr>
				<th>編集前の生徒名</th>
				<td th:text ="${student.studentName}"></td>
			</tr>
			<tr>
				<th>編集前の学年</th>
				<td th:switch="${student.grade}">
					<span th:case="5">小５</span>
					<span th:case="6">小６</span>
					<span th:case="7">中１</span>
					<span th:case="8">中２</span>
					<span th:case="9">中３</span>
					<span th:case="10">高１</span>
					<span th:case="11">高２</span>
					<span th:case="12">高３</span>
				</td>
			</tr>
		</table>
		<br>
		<table border="1">
			<tr>
				<th>生徒の名前</th>
				<td>
					<input type="text" th:field="*{subjectName}">
					<span th:if="${#fields.hasErrors('studentName')}"
					th:errors="*{subjectName}" style="color:red">
					</span>
				</td>
			</tr>
			<p 	th:if ="${errorMessage}"
				th:text="${errorMessage}" style ="color: red">
		</table>
		<input th:if="${subjectForm.isNewSub}" type="submit" value="登録">
		<input th:unless="${studentForm.isNewSub}" type="submit" value="更新">
	</form>
	<a th:href="@{/subjects}">戻る</a>
</body>
</html>
