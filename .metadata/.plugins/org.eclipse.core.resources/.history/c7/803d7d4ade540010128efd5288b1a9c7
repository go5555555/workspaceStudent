<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<meta charset="UTF-8">
<title>生徒フォーム</title>
</head>
<body>
	<h2 th:if="${dayForm.isNewDay}">日付登録</h2>
	<h2 th:unless="${dayForm.isNewDay}">日付編集</h2>
	
	<form th:action="${dayForm.isNewDay}? @{/days/save}:@{/days/update}"
		th:object="${dayForm}" method="post">
		<input type="hidden" th:field="*{studentId}">
		<table>
			<tr>
				<th>生徒名</th>
				<td th:text="${dayForm.studentName}"></td>
			</tr>
			<tr>
				<th th:unless="${dayForm.isNewDay}">更新する日付</th>
				<td th:unless="${dayForm.isNewDay}" th:text="${dayForm.oldStudyDay}"></td>
			</tr>
<!--		<p th:text="${dayForm.studentName}">生徒名</p>-->
<!--		<p th:unless="${dayForm.isNewDay}" th:text="${dayForm.oldStudyDay}">更新前の日付</p>-->
		</table>
		<table>
			
			<tr>
				<th>学習日</th>
				<td>
					<input type="date" th:field="*{studyDay}" />
						<span th:if="${#fields.hasErrors('studyDay')}"
							th:errors="*{studyDay}" style="color:red">
						</span>
					
				</td>
			</tr>
		</table>
		<input th:if="${dayForm.isNewDay}" type="submit" value="登録">
		<input th:unless="${dayForm.isNewDay}" type="submit" value="更新">
	</form>
	<a th:href="@{/days/{id}(id=${dayForm.studentId})}">戻る</a>	
</body>
</html>
